# OC3 Company World Art

The runtime renders the approved pixel-office artwork from a static browser asset and overlays PixiJS interaction/state layers on top. Keeping the artwork as a normal public asset avoids browser/Pixi loader failures caused by very large inline data URIs.
